spring:
  jpa:
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
  application:
    name: mercadolibro-service
  flyway:
    baseline-on-migrate: 'false'
  datasource:
    password: baeMoo2l
    username: pi21023c01_GRUPO5
    url: jdbc:mysql://db.ctd.academy:3306/pi21023c01_GRUPO5?autoReconnect=true&useSSL=false
server:
  error:
    include-stacktrace: never
  port: 8080
  servlet:
    context-path: /v1
logging:
  level:
    org:
      hibernate:
        SQL: DEBUG

#eureka:
#  instance:
#    instance-id: ${spring.application.name}:${spring.application.instance_id:${random.value}}
#  client:
#    service-url:
#      defaultZone: http://localhost:8761/eureka